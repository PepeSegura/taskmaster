-Logging system
-Yaml parsing to variables, see subject/config example.
-Sighup reloads config but not processes
-Control shell with at least:
    - status
    - start /stop /restart
    - reload config
    - stop supervisor

struct for each process, must have
    - cmd name
    - cmd instance and pid
    - date first time it was launched
    - signal to stop it
